import { Box } from "@mui/material";

const Feed = () => {
  return (
    <div>
      <Box sx={{ p: 2, border: "1px dashed grey", color: "red" }}>
        This is a section container
      </Box>
    </div>
  );
};

export default Feed;
